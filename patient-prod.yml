server:
  port: 9000

  application:
    name: "patient"
  profiles:
    active: "prod"
  datasource:
    url: jdbc:mysql://localhost:3308/patientdb
    username: root
    password: root
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
