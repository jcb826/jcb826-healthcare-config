server:
  port: 9000
  datasource:
    url: jdbc:mysql://localhost:3308/patientdb
    username: root
    password: root
  application:
    name: "patient"
  profiles:
    active: "prod"
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
