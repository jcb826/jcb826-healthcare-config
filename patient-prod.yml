server:
  port: 9020
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: true
        health-check-path: /actuator/health
        health-check-interval: 15s
  datasource:
    url: jdbc:mysql://localhost:3308/patientdb
    username: root
    password: root 
